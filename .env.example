# Mehana Manastira Sveti Vlas — environment (copy to `.env.local`)

# EmailJS — create a template that greets `{{to_name}}` and lists all fields below.
NEXT_PUBLIC_EMAILJS_SERVICE_ID=
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=

# Optional: set the production site URL for metadata (defaults are set in layout.tsx)
# NEXT_PUBLIC_SITE_URL=https://www.mehana-manastira-svetivlas.bg
