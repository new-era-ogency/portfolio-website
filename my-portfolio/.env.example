# Скопируйте этот файл в `.env` или `.env.local` (оба не коммитятся) и подставьте значения
# из Supabase: Dashboard → Project Settings → API.
# Vite читает только переменные с префиксом VITE_. Приоритет: `.env.local` перекрывает `.env`.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=